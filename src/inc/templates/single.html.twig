<!DOCTYPE html>
<html>
<head>
    <title>{{ current_post.post_title }}</title>
</head>
<body>
    <h1>{{ current_post.post_title }}</h1>
    <div>{{ current_post.post_content }}</div>
</body>
</html>
