{% extends "base.html.twig" %}

{% block content %}
<h1>Latest Posts</h1>
<ul>
    {% for post in archive.posts %}
        <li>
            <a href="{{ post.url }}">{{ post.title }}</a>
            <p>{{ post.excerpt }}</p>
        </li>
    {% endfor %}
</ul>
{% endblock %}